<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="../../stylesheets/main.css">
    <% include ../../partials/head %>
  </head>
  <body>
    <header>
      <% include ../../partials/header %>
    </header>
      <h1>Maryland Skateparks</h1>
      <% if(parks.length>0) { %>
      <main>
          <ul>
          <% for(var i=0; i<parks.length;i++){if(parks[i].state == "MD"){%>
              <li><a href="/show/<%= parks[i]._id%>"><%= parks[i].name%></a></li> 
          <% } else { %> <li>No parks have been added yet.</li> <% }} %>
          </ul>
          <% } %>
          </ul>
      </main>
      <div><h2><a href="/">Back to States.</a></h2></div>
    <footer class="m-all t-all d-all">
        <% include ../../partials/footer %>
    </footer>
    <% include ../../partials/scripts %>
    <script type="text/javascript" src="../../js/scripts.js"></script>
  </body>
</html>