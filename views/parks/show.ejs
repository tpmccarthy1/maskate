<!DOCTYPE html>
<html>
  <head>
    <head>
    <% include ../partials/head %>
    <link rel="stylesheet" href="../../stylesheets/main.css">
    </head>
  </head>
  <body>
    <header>
      <% include ../partials/header %>
    </header>
    <main>
    <div class="m-all t-all d-allshow-park row clearfix">   
        <div class="intro col">
        <h3><a href="/">Back to Skatepark List</a></h3>
        <h1>Park Detail</h1>
        </div>
        <div class="park-info m-all t-all d-1of2">
        <ul>
            <li>
              <span class="label">Name: </span><span><%= park.name %></span>
            </li>
            <li>
              <span class="label">Street: </span><span><%= park.street %></span>
            </li>
            <li>
              <span class="label">Town: </span><span><%= park.town %></span>
            </li>
            <li>
              <span class="label">State: </span><span><%= park.state %></span>
            </li>
            <li>
              <span class="label">County: </span><span><%= park.county %></span>
            </li>
            <li>
              <span class="label">Hours: </span><span><%= park.hours %></span>
            </li>
             <li>
              <span class="label">Hemlets: </span><span><%= park.helmets %></span>
            </li>
            <li>
              <span class="label">Description: </span><span><%= park.description %></span>
            </li>
            <li>
              <div class="img-container">
              <span>Images: </span><p><img src= "https://s3.amazonaws.com/maskate/<%= park.imgs.img0 %>" /></p>
              <p><img src= "https://s3.amazonaws.com/maskate/<%= park.imgs.img1 %>" /></p>
              <p><img src= "https://s3.amazonaws.com/maskate/<%= park.imgs.img2 %>" /></p>
              <p><img src= "https://s3.amazonaws.com/maskate/<%= park.imgs.img3 %>" /></p>
              <p><img src= "https://s3.amazonaws.com/maskate/<%= park.imgs.img4 %>" /></p>
              </div>
            </li>
        </ul>
        </div>
        <!-- google maps -->
          <div class="m-all t-all d-1of2">
            <p>Google maps goes here</p>
          </div>
    </div>
<!--       <h3><a href="/parks/edit/<%= park._id%>">EDIT</a></h3>
      <form action="/parks/delete/<%= park._id%>" method="post">
        <button type="submit">DELETE</button>
      </form> -->
    </main>
    <footer class="m-all t-all d-all">
        <% include ../partials/footer %>
    </footer>
    <% include ../partials/scripts %>
    <script type="text/javascript" src="../../js/scripts.js"></script>
  </body>
</html>